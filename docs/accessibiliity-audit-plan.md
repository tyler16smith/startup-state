I completed a read-only WCAG 2.1 AA audit of the web app and found no evidence of a single catastrophic P0 blocker, but there are several P1 issues that will materially affect keyboard and screen reader users in core flows.

Prioritized Remediation Plan

Priority	Files	Issue	Affected Users	WCAG	Suggested Fix
P1	startup-layout-shell.tsx:44, startup-state-ai-workspace.tsx:61, sheet.tsx:110	SheetContent is used without SheetTitle/description for mobile navigation and the mobile Agent panel. Radix Dialog-backed sheets need an accessible name.	Screen reader users opening mobile nav/Agent	4.1.2, 2.4.6	Add a visible or sr-only SheetHeader + SheetTitle inside each SheetContent, e.g. “Navigation” and “Startup State Agent”. Add SheetDescription where useful.
P1	dropdown-autocomplete.tsx:200, hashtag-combobox.tsx:200, company-form.tsx:508, resource-form.tsx:334	Custom autocomplete/comboboxes are visually usable but do not expose combobox/listbox semantics, active option, expanded state, controls, or accessible labels.	Screen reader and keyboard users filtering/creating companies/resources/tags	4.1.2, 1.3.1, 2.1.1	Prefer shadcn/Radix Command/Popover combobox pattern, or implement role="combobox", aria-expanded, aria-controls, aria-activedescendant, stable option ids, role="listbox"/"option", and pass id/label props from callers.
P1	company-form.tsx:201, resource-form.tsx:157, claim-company-form.tsx:50, mcp-settings-section.tsx/settings/mcp-settings-section.tsx#L301)	Many visible Label components are not associated with controls via htmlFor/id; some select, Input, Textarea, and custom controls rely on proximity only.	Screen reader, speech input, cognitive users	1.3.1, 3.3.2, 4.1.2	Give every field a stable id; set Label htmlFor; for Radix Select, connect label via aria-labelledby or shadcn Form. For grouped checkboxes/autocomplete fields, use fieldset/legend or an accessible group label.
P1	company-form.tsx:146, resource-form.tsx:204, page.tsx:158, page.tsx:207	Form errors and success messages are plain paragraphs. They are not associated with fields, not announced reliably, and focus is not moved to the first error. Required fields are not consistently indicated beyond native required.	Screen reader and keyboard users completing auth/admin/submission forms	3.3.1, 3.3.2, 4.1.3	Add role="alert"/aria-live for form-level messages, aria-invalid + aria-describedby for field errors, visible required indicators, and focus the first invalid field or an error summary after failed submit.
P1	filter-select.tsx:182	The map filter clear “X” is an SVG with onClick inside a button, with aria-label on the SVG. It is not a separate keyboard target and mixes trigger/clear actions.	Keyboard and screen reader users clearing map filters	2.1.1, 4.1.2	Replace the SVG clear affordance with a real nested-adjacent button type="button" outside the trigger button, or split the pill into trigger and clear button. Keep aria-expanded on the trigger and a specific clear label on the clear button.
P1	map/page.tsx/map/page.tsx#L4), page.tsx:62, page.tsx:83, card.tsx:29	Several full pages have no semantic h1; auth pages use CardTitle, which renders a div, and the map page renders only the map.	Screen reader and keyboard users navigating by headings	1.3.1, 2.4.6	Add exactly one meaningful h1 per page. For visual designs that should stay compact, use className="sr-only". Consider changing CardTitle use on page-level cards to actual heading elements or accepting asChild.
P1	startup-layout-shell.tsx:29, layout.tsx:25, settings/page.tsx/settings/page.tsx#L13)	Repeated sidebar/header navigation has no skip-to-content link, and some pages such as settings return a div rather than their own main landmark.	Keyboard and screen reader users traversing repeated nav	2.4.1, 1.3.1	Add a focusable skip link at the root/startup shell targeting the main content. Ensure each route has one main landmark, either in the shell or page, not both.
P1	option-grid.tsx:26, founder-intake-form.tsx:240, investor-intake-form.tsx:211	Multi/single choice option grids are sets of buttons with selection shown mainly by color/border and no programmatic selected state.	Screen reader, color-blind, keyboard users in founder/investor intake	1.3.1, 1.4.1, 4.1.2	For multi-select groups, use checkboxes styled as cards or buttons with aria-pressed; for single-select, use radio groups. Add group labels via fieldset/legend. Preserve visible non-color selected state.
P2	resource-filter-panel.tsx:151, resource-directory-results.tsx:37	Search input has only placeholder text, filter result updates are visual only, and the “Updating results” overlay is not a live status.	Screen reader users filtering resources	3.3.2, 4.1.3	Add a visible or sr-only label for search, role="status"/aria-live="polite" for result count and pending state, and keep results region identifiable with aria-busy during transitions.
P2	csv-import-form.tsx:91, resource-csv-import-form.tsx:107	Hidden file inputs are triggered from buttons, but drag-and-drop import is global and the hidden input lacks an accessible label/description. Drag overlay is not announced.	Keyboard, screen reader, switch users importing CSVs	2.1.1, 3.3.2, 4.1.3	Prefer a real labeled file input visually styled as a button, or add id/label and helper text. Add aria-live messages for import status and drag/drop state; ensure all drag flows have button/input equivalents.
P2	admin/companies/page.tsx/admin/companies/page.tsx#L35), admin/resources/page.tsx/admin/resources/page.tsx#L29), admin/claims/page.tsx/admin/claims/page.tsx#L40), resource-csv-import-form.tsx:166	Tables use native table markup, which is good, but lack captions/descriptions and action columns have generic “Edit” labels repeated by row.	Screen reader users in admin data-heavy workflows	1.3.1, 2.4.6	Add TableCaption or aria-label for each table. Make repeated row actions specific, e.g. “Edit Acme Co”. Use scope="col" on header cells if the table component does not already guarantee it.
P2	startup-sidebar.tsx:49, page-breadcrumb.tsx:12	Sidebar active links are visually active but do not expose current page state. Breadcrumb appears to handle aria-label, but nav links should also expose aria-current.	Screen reader users orienting in navigation	2.4.4, 2.4.8	Add aria-current="page" to active sidebar and breadcrumb current links/items.
P2	fullscreen-button.tsx:15, map-search-input.tsx:18, filter-select.tsx:171	Several custom map controls remove default outline and use subtle focus rings; search input is unlabeled except placeholder.	Low-vision and keyboard users	2.4.7, 1.4.11, 3.3.2	Standardize visible focus rings with sufficient contrast and add labels for map search/filter controls. Verify focus is visible against map backgrounds.
P2	globals.css:53, page.tsx:211, startup-sidebar.tsx:174	Low-contrast text patterns appear in small text, especially text-gray-400, text-slate-500, text-muted-foreground/70, disabled opacity, and text over tinted badges/cards.	Low-vision users, users in bright light/high zoom	1.4.3, 1.4.11	Run contrast checks for light/dark themes. Replace very light small text with tokenized AA colors; avoid opacity for important disabled/help text unless contrast remains compliant.
P2	navigator-shell.tsx:52, dialog.tsx:62, sheet.tsx:64, globals.css:2	Animations/transitions are common; only one flow checks reduced motion directly.	Vestibular/motion-sensitive users	2.3.3 advisory, practical AA polish	Add global prefers-reduced-motion: reduce handling for transition/animation utilities or apply motion-reduce:* classes to dialogs, sheets, popovers, map/fullscreen transitions, and skeletons.
P2	chat-composer.tsx:30, message-list.tsx:17	Agent textarea has no visible label, uses placeholder-only naming, and streamed/new messages are not announced as live updates.	Screen reader users using Agent	3.3.2, 4.1.3	Add a visible or sr-only label for agent-input; expose message list as a named log/status region (role="log" with careful politeness) and announce run state.
P3	company-card.tsx:14, companies/[id]/page.tsx, logo.tsx:20	Image alt text exists, but some company photos may default to only the company name and the logo may be redundant when adjacent text repeats the brand.	Screen reader users consuming cards/profiles	1.1.1	Decide per image: decorative photos can use empty alt; meaningful photos need descriptive alt. For logo next to visible brand text, consider empty alt or ensure the link name is not duplicated awkwardly.
P3	package.json:1, biome.jsonc:22	No eslint-plugin-jsx-a11y, axe, jest-axe, or Playwright a11y setup is configured for the web app. Biome has linting, but there is no dedicated accessibility test gate.	Engineering quality, regression prevention	Testing support	Add either ESLint with eslint-plugin-jsx-a11y or explicitly tune Biome a11y rules. Add @axe-core/playwright smoke tests for auth, resources, company submission, map, settings dialogs, and admin tables.
Execution Order

Fix structural blockers first: sheet titles, page h1s, main landmarks, skip link.
Fix forms next: label associations, field errors, required indicators, live form feedback.
Replace or repair custom comboboxes and option grids.
Improve dynamic updates: resource filtering, Agent streaming, CSV import, loading overlays.
Audit contrast and focus visually in browser across light/dark and map backgrounds.
Add automated checks, then manually test keyboard-only and VoiceOver on the top flows.